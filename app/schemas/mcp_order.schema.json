{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MCPOrder",
  "type": "object",
  "required": ["user", "intent", "target"],
  "properties": {
    "user": {
      "type": "string",
      "description": "Identificador del usuario que envía la orden"
    },
    "intent": {
      "type": "string",
      "description": "Acción a ejecutar sobre la entidad. Basado en los servicios oficiales de Home Assistant.",
      "enum": [
        "turn_on", "turn_off", "set_brightness", "set_color", "play_media", "pause", "stop", "volume_set", "select_source", "start", "return_to_base", "set_fan_speed", "clean_spot", "notify"
      ]
    },
    "target": {
      "type": "string",
      "description": "Entidad objetivo de la acción (formato: domain.entity_id, ej: light.king_s_bedroom)",
      "pattern": "^[a-zA-Z_]+\\.[a-zA-Z0-9_]+$"
    },
    "context": {
      "$ref": "#/definitions/Context"
    },
    "version": {
      "type": "string",
      "description": "Versión del schema MCPOrder"
    }
  },
  "definitions": {
    "Context": {
      "type": "object",
      "properties": {
        "location": { "type": "string", "description": "Ubicación física o lógica del usuario/dispositivo" },
        "time": { "type": "string", "format": "date-time", "description": "Fecha y hora de la orden" },
        "area": { "type": "string", "description": "Área de la casa o ambiente (ej: King's Bedroom, Living, Studio)" },
        "extra": { "type": "object", "description": "Propiedades adicionales contextuales" }
      },
      "additionalProperties": { "type": "string" },
      "description": "Información contextual relevante para la orden."
    }
  },
  "additionalProperties": false,
  "description": "Schema para órdenes MCP dirigidas a Home Assistant u otros servicios compatibles."
} 